on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:
jobs:
  speak:
    runs-on: ubuntu-latest
    steps:
      - name: speak
        run: echo "Hello, World!"
